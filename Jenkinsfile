pipeline {
  agent any
  stages {
    stage('Java Version') {
      parallel {
        stage('Java Version') {
          steps {
            bat 'java -version'
          }
        }

        stage('Java Version') {
          steps {
            bat 'java -version'
          }
        }

      }
    }
    stage('Running Test') {
      steps {
        bat 'Exit'
      }
    }

  }
}
